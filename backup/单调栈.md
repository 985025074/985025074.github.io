单调栈，用于解决寻找左右两边比他大的 或者比他小的 最值。  
# 如何理解？
关键点是弹出的时机，弹出的时候被弹出的数遇到了一个比他小或者比他大的数，这个数就是那个方向的第一个。  
如何理解？ 以寻找右边第一个比他的数字为例。我们从左往右遍历。这样某个数字弹出的时候是被右边的数字检测到的比他大，故而找的是右边的数字。
## 如果X 被弹出，有没有可能在X 后面的数字（后入栈的）是第一个更大值？
不可能 如果这样，之前的单调性就破坏了。